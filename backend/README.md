# Mentor MVP Backend

Simple Express backend that serves a hardcoded AI-style analysis at POST /analyze.

Run:

1. cd backend
2. npm install
3. npm start

The server listens on port 4000 by default and returns a JSON analysis.
