# Mentor MVP Frontend

This is a Vite + React frontend that talks to the backend at http://localhost:4000/analyze

Run:

1. cd frontend
2. npm install
3. npm run dev

Open http://localhost:3000 in your browser.
